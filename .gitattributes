* text=auto eol=lf

# Force LF for shell scripts (critical for Docker containers)
*.sh text eol=lf

# Force LF for Docker/config files
Dockerfile text eol=lf
docker-compose.yml text eol=lf
nginx/*.conf text eol=lf
